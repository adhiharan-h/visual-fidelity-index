# Redirect www to apex domain
# https://www.visualfidelityindex.com/*  https://visualfidelityindex.com/:splat  301

# Note: SPA fallback removed — this is a static site, not a SPA.
# Cloudflare Workers handles 404s via not_found_handling in wrangler.jsonc.
